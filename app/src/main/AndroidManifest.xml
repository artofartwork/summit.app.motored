<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.movil.summmit.motorresapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".ListaInformesActivity" />
        <activity android:name=".NuevoInformeActivity" />
        <activity android:name=".AnalisisCausaFallaActivity" />
        <activity android:name=".DetalleCausaAnalisisFallaActivity"></activity>
        <activity android:name=".ConclusionesRecomendacionesActivity" />
        <activity android:name=".AdjuntosActivity" />
        <activity android:name=".GuardarEnviarActivity" />
        <activity android:name=".EditarInformeTecnicoActivity" />
        <activity android:name=".FilePickActivity" />
        <activity android:name=".DetailInformeTecnicoActivity" />
        <activity android:name=".DetailTwoActivity" />
        <activity android:name=".EditarDetalleCausaActivity"></activity>
    </application>

</manifest>